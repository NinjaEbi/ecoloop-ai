# Testing

Backend tests cover health, persisted deterministic assessment, all five option scores, and MIME rejection. Frontend uses a Vitest smoke test and production build. Manual browser checks should cover home, valid upload, manual fallback, black-screen symptom answers, invalid/dark/blurred images, history persistence, and visible result breakdown. Record actual command output in the final project report.
