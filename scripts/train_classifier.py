"""Training entry point placeholder. Refuses to claim training before an inspected manifest and selected runtime are provided."""
raise SystemExit('No training run was started: provide an inspected dataset report, approved mapping manifest, and a selected YOLO runtime. See docs/ml_pipeline.md.')
